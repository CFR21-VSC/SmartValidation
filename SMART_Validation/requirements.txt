# Python 3.11+ required.
# psycopg2-binary is only used when DATABASE_URL is set (PostgreSQL / Railway).
# In local dev (no DATABASE_URL), only the standard library is needed.
# Versión fijada (SEC-FIX-DEPS01) -- ver requirements.txt de la raíz del repo.
psycopg2-binary==2.9.12
