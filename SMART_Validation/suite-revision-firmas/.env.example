# Local: dejar DATABASE_URL vacío usa SQLite (./data/revision_firmas.db)
DATABASE_URL=

RF_AUTH_SECRET_KEY=dev-secret-change-me
RF_APP_BASE_URL=http://localhost:8090
RF_TOKEN_EXPIRE_H=12
RF_INVITE_TTL_H=72

RF_SUPERADMIN_USERNAME=
RF_SUPERADMIN_PASSWORD=
RF_SUPERADMIN_EMAIL=
RF_SUPERADMIN_DISPLAY=DRP Admin

RESEND_API_KEY=
RF_FROM_EMAIL=no-reply@drpassurance.com

# Secreto compartido con la Suite Documental (server.py) para el bridge servicio-a-servicio
# (router bridge.py). Debe tener el MISMO valor en las env vars de ese otro servicio.
# Generar con: python -c "import secrets; print(secrets.token_hex(32))"
BRIDGE_API_KEY=
